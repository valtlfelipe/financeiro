# Instalação self-hosted. Este arquivo só fornece variáveis ao Docker Compose.
# Execute scripts/init-env.sh para gerar os segredos e depois ajuste APP_URL.
APP_KEY=
DB_PASSWORD=
APP_URL=http://localhost:8080
